name: Test
on: pull_request

jobs:
  running-test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: run
        uses: ./
        with:
          project_id: 391
          testplan_id: 32135
          personal_token: ${{ secrets.AUTIFY_PERSONAL_TOKEN }}
